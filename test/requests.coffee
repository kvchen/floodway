expect   = require('chai').expect
nconf    = require 'nconf'

app = require '../app'
Request = require '../app/models/request'


describe 'Request methods', ->
	it 'should create a new request'
	it 'should remove an existing request'
	it 'should modify an existing request'
	it 'should not modify a nonexistent request'
